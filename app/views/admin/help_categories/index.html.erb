<%- model_class = HelpCategory -%>
<% title t '.title', :default => model_class.model_name.human(:count => 2) %>

<div class="form-group form-group-admin-buttons">
  <%= link_to t('.new', :default => t("helpers.links.new")),
              new_admin_help_category_path,
              :class => 'btn btn-primary' %>
  <%= link_to t('helpers.links.admin'), admin_path,
  :class => 'btn btn-default' %>
</div>


<table class="table table-striped display">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:name) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @help_categories.each do |help_category| %>
      <tr>
        <td><%= help_category.name %></td>
        <td>
          <%= link_to '',
                      edit_admin_help_category_path(help_category),
                      :title => t('helpers.links.edit'),
                      :class => 'btn btn-xs btn-edit' %>
          <%= link_to '',
                      admin_help_category_path(help_category),
                      :title => t("helpers.links.destroy"),
                      :method => :delete,
                      :data => { :confirm => I18n.t("helpers.links.confirm") },
                      :class => 'btn btn-xs btn-delete' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
