    <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
          <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-collapse">
            <span class="sr-only"><%= t('.toggle') %></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <button type="button" class="navbar-toggle">
            <img src="/assets/svg/language.svg">
          </button>
          <%
            link_class = ''
            img_path = 'svg/logo.svg'
            if @is_xtraktr
              link_class = 'xtraktr'
              img_path = 'svg/xtraktr_logo.svg'
            end
          %>
          <%= link_to root_path(:locale => I18n.locale),:class=>"navbar-brand #{link_class}" do %>
            <%= image_tag img_path, :id => 'logo', :alt => t("app.common.#{@app_key_name}.app_name") %>
          <% end %>
        </div>
        <div class="stats navbar-left">
          <div class="img"></div>
          <div class="text">
            <div class="number"><%= number_with_delimiter(@public_question_count) %></div>
            <div class="questions"><%= t('app.common.questions') %></div>
          </div>
        </div>
        <% I18n.available_locales.each do |locale| %>
          <% if locale != I18n.locale %>
            <div class="lang-icon"><%= link_to t("app.language.#{locale}"), params.merge(:locale => locale) %></div>
          <% end %>
        <% end %>
        <form class="navbar-form navbar-left" role="search" action="<%= explore_data_path %>">
          <div class="form-group">
            <input type="search" class="form-control" placeholder="<%= t('helpers.search') %>" name="q">
          </div>
        </form>

      <ul class="nav navbar-right navbar-top-links">
        
        <% if user_signed_in? %>
          <li class="dropdown user">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= current_user.name %>
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu" role="menu">
              <% if current_user.role?(@data_editor_role) %>
                <li><%= link_to datasets_path do %>
                  <i class="menu_datasets"></i>
                  <%= t('app.menu.datasets') %>
                  <% end %>
                </li>
                <li><%= link_to time_series_index_path do %>
                  <i class="menu_time_series"></i>
                  <%= t('app.menu.time_series') %>
                  <% end %></li>
              <% end %>
              <li><%= link_to settings_path  do %>
                  <i class="menu_user_settings"></i>
                  <%= t('app.menu.user_settings') %>
                  <% end %></li>
              <% if current_user.role?(@site_admin_role) %>
                <li><%= link_to admin_path  do %>
                  <i class="menu_admin"></i>
                  <%= t('helpers.links.admin') %>
                  <% end %></li>
              <% end %>
            </ul>
          </li><li class="user">
            <%= link_to destroy_user_session_path, :method => :delete, title: t('helpers.links.sign_out'), class:"signout" do %>
              <%= image_tag 'svg/signout.svg', :alt => t('helpers.links.sign_out') %>
            <% end %>
          </li>
        <% else %>
          <li role="menuitem" class="sign user">
            <%= link_to(t('helpers.links.sign_in2'), new_user_session_path, class: 'reattach sign_in')  %></li>
            <li class="user">/</li>
            <li role="menuitem" class="sign user"><%= link_to(t('helpers.links.sign_up'),  new_registration_path(resource_name), class: 'reattach sign_in')  %></li>
        <% end %>
        <% I18n.available_locales.each do |locale| %>
          <% if locale != I18n.locale %>
            <li class="lang-link"><%= link_to t("app.language.#{locale}"), params.merge(:locale => locale) %></li>
          <% end %>
        <% end %>
      </ul>

      <div class="navbar-default sidebar" role="navigation">
        <div class="sidebar-nav navbar-collapse">
          <ul class="nav in" id="side-menu">          
            <% if !@is_xtraktr %>
            <li><%= link_to t('.back'), 'http://www.unicef.ge', class: 'unicef', :target => :blank %></li>
            <% end %>
            <li><%= link_to_sidebar root_path, 'home' %></li>
            <li><%= link_to_sidebar explore_data_path, 'datasets' %></li> 
            <% if @is_xtraktr %>
              <li><%= link_to_sidebar explore_time_path, 'time_series' %></li>            
            <% end %>            
            <li><%= link_to_sidebar instructions_path, 'instructions' %></li>
            <li><%= link_to_sidebar api_path, 'api' %></li>
            <li><%= link_to_sidebar contact_path, 'contact' %></li>                                        
          </ul>
          <div class="sidebar-bottom">
            <div class="share">
              <%= link_to '', "https://www.facebook.com/sharer/sharer.php?u=" +  root_url, class: 'facebook', onclick:"javascript:window.open(this.href,
                          '', 'menubar=no,toolbar=no,resizable=yes,scrollbars=yes,height=600,width=600');return false;" %>
              <% title = content_for?(:title) ? "#{yield(:title)} | #{t("app.common.#{@app_key_name}.app_name")}" : "#{t("app.common.#{@app_key_name}.app_name")} | #{t("app.common.#{@app_key_name}.app_slogan")}".html_safe %>
              <%= link_to '',"https://twitter.com/share?text=#{title}" , class: 'twitter', onclick:"javascript:window.open(this.href,
                          '', 'menubar=no,toolbar=no,resizable=yes,scrollbars=yes,height=600,width=600');return false;" %>
            </div>
            <%= link_to t('app.menu.disclaimer'), disclaimer_path, class: 'disclaimer' %>
          </div>   
        </div>
      </div>
    </nav>