<%= yield %>


<%= t('mailer.closing', :locale => @message.locale) %>
<%= t('mailer.signature', :locale => @message.locale) %>


-------------------------------

<%= "#{t('mailer.unsubscribe', :locale => @message.locale)}: #{settings_from_notification_url(:locale => @message.locale)}" %>
